import { RectangleDragCloseDirective } from './rectangle-drag-close.directive';

describe('RectangleDragCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new RectangleDragCloseDirective();
    expect(directive).toBeTruthy();
  });
});
