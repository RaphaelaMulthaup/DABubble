import { BaseSearchDirective } from './base-search.directive';

describe('BaseSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseSearchDirective();
    expect(directive).toBeTruthy();
  });
});
